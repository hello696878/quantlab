"use client";

/**
 * @deprecated Use `@/components/ui/OfflineState` directly.  Kept as a re-export
 * so any lingering import keeps working; the canonical offline panel now lives
 * in the shared `ui/` state primitives.
 */
export { default } from "@/components/ui/OfflineState";
